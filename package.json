{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "dependencies": {
    "@fontsource/roboto": "^5.0.1"
  },
  "devDependencies": {
    "turbo": "^1.10.0"
  }
}
